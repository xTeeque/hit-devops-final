# registry.npmjs.org refuses the TLS handshake from this network, so we use the
# public Yarn mirror of the same registry. Remove this line if npmjs is reachable.
registry=https://registry.yarnpkg.com
